package com.calculator.presentation;

public interface iUserInterface {

    void start();

}

// method returns nothing it only starts the loop, so void
// start, the ui takes over and handles input, displaying result, loop until quit
//
